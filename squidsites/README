Using squidsites:

Example:

Generate a report on console, sorting sites by hit count.
  squidsites < /var/log/squid/access.log

Generate a report on console, sorting sites by bytes count.
  squidsites -b < /var/log/squid/access.log

Generate a report on file report.txt, sorting sites by bytes count.
  squidsites -b < /var/log/squid/access.log > report.txt

You can also use squidsites as a filter:
  cat /var/log/squid/access.log | squidsites > report.txt


# $Id:
# Description:	An essential exemplary set of plugins for gstreamer.
# URL:			http://gstreamer.freedesktop.org/modules/gst-plugins-base.html
# Maintainer:	Danny Rawlins, <romster@shortcircuit.net.au>
# Packager:		Matt Housh, <jaeger at morpheus dot net>
# Depends on:	liboil, libtheora, cdparanoia

# Recommended:	libvisual
# Optional:	
# Category:		audio

name=gstreamer-plugins-base
version=0.10.10
release=1
source=(http://gstreamer.freedesktop.org/src/gst-plugins-base/gst-plugins-base-$version.tar.gz)

build() {
	cd gst-plugins-base-$version

	./configure \
		--prefix=/usr \
		--disable-nls

	make && make DESTDIR=$PKG install
	chown -R root:root $PKG
}


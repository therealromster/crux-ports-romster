# $Id: 
# Description: Libary for Galago.
# URL: http://galago-project.org/
# Maintainer: Danny Rawlins, <romster@shortcircuit.net.au>
# Packager: Danny Rawlins, <romster@shortcircuit.net.au>
# Depends on: dbus-glib

name=libnotify
version=0.4.2
release=1
source=(http://galago-project.org/files/releases/source/libnotify/libnotify-$version.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr
	make && make DESTDIR=$PKG install
	rm -fr $PKG/usr/share/gtk-doc
	chown -R root:root $PKG
}

# Description: Filesystem benchmark tool.
# URL: http://www.iozone.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Danny Rawlins, romster at shortcircuit dot net dot au
# Depends on: 

# Recommended:	
# Optional:	
# Category:	filesystem

name=iozone
version=3.283
release=1
source=(http://www.iozone.org/src/current/iozone${version/./_}.tar)

build() {
	tar -xf iozone${version/./_}.tar
	cd iozone${version/./_}/src/current
	make linux
	install -d $PKG/usr/bin
	install -m755 iozone fileop -t $PKG/usr/bin/
	install -D -m644 ../../docs/iozone.1 $PKG/usr/man/man1/iozone.1
}


# $Id:
# Description:	Base64 encoder/decoder.
# URL:			http://www.fourmilab.ch/webtools/base64/
# Packager:		Jürgen Daubert, juergen dot daubert at t-online dot de
# Maintainer:	Danny Rawlins, <romster@shortcircuit.net.au>

name=base64
version=1.4
release=1
source=(http://www.fourmilab.ch/webtools/$name/$name-$version.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr
	install -d $PKG/usr/{bin,man/man1}
	make && make DESTDIR=$PKG install
	chown -R root:root $PKG
}


# $Id: 
# Description: LZMA Compression as used by 7-zip.
# URL: http://tukaani.org/lzma/
# Packager: Danny Rawlins, <romster@shortcircuit.net.au>
# Maintainer: Danny Rawlins, <romster@shortcircuit.net.au>
# Depends on:

# Recommended:	
# Optional:	
# Category:	compression, algorithm

name=lzma
version=4.32.0beta3
release=1
source=(http://tukaani.org/$name/$name-$version.tar.gz)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--disable-static

	make && make prefix=$PKG/usr install
	chown -R root:root $PKG
}

# Description: A Linux kernel packet filter control tool.
# URL: http://www.iptables.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: CRUX System Team, core-ports at crux dot nu
# Core: glibc

name=iptables
version=1.4.0
release=2
source=(ftp://ftp.netfilter.org/pub/iptables/iptables-$version.tar.bz2)

build() {
    cd iptables-$version
    make KERNEL_DIR=/usr PREFIX=/usr COPT_FLAGS="$CFLAGS"
    make KERNEL_DIR=/usr PREFIX=/usr DESTDIR=$PKG install install-devel
}


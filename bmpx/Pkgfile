# Description: Next generation rewrite of BMP.
# URL: http://beep-media-player.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Matt Housh, jaeger at morpheus dot net
# Depends on: boost cairomm gamin gstreamer-plugins-bad gstreamer-plugins-good gstreamer-plugins-ugly hal libglade libglademm libmikmod librsvg sqlite3 taglib xine-lib

# Recommended:	
# Optional:	dbus, gconf, startup-notification, smlib
# Category:	audio, player

name=bmpx
version=0.34.9
release=1
source=(http://files.beep-media-player.org/releases/${version::4}/$name-$version.tar.bz2)

build() {
	cd $name-$version
	GST_PLUGINS_BASE_LIBS=/usr/lib
	
	./configure \
		--prefix=/usr \
		--libexecdir=/usr/lib \
		--disable-nls \
		--enable-python
		
	make && make DESTDIR=$PKG install

	rm -r $PKG/usr/share/locale
		
	rm \
		$PKG/usr/bin/bmp2 \
		$PKG/usr/share/bmpx/data/GPL.txt
		
	ln -s /usr/bin/beep-media-player-2 $PKG/usr/bin/bmp2
}


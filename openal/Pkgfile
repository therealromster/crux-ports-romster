# Description: Cross-platform 3D audio API (snapshot).
# URL: http://openal.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Han Boetes, han at mijncomputer dot nl
# Depends on: alsa-lib libvorbis libsdl

name=openal
version=r1426
release=1
source=(http://romster.shortcircuit.net.au/crux/source/openal-snapshot-$version.tar.bz2)

build() {
	cd openal

	./configure \
		--prefix=/usr \
		--disable-static \
		--disable-oss \
		--with-gnu-ld

	#sed -i -e 's|^DESTDIR.*|# &|' Makefile
	make
	make DESTDIR=$PKG install
}


# Description: conntrack, the userspace command line interface, and conntrackd, the userspace daemon.
# URL: http://people.netfilter.org/pablo/conntrack-tools/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Danny Rawlins, romster at shortcircuit dot net dot au
# Depends on: libnetfilter_conntrack libnfnetlink

name=conntrack-tools
version=0.9.8
release=1
url="http://www.netfilter.org/projects/$name/files"
up2date="lynx -dump '$url/?C=M;O=D' |grep tar.bz2$ |sed -ne 's/.*-\(.*\)\.t.*/\1/' -e '1 p'"
source=($url/$name-$version.tar.bz2
	conntrackd.rc)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--mandir=/usr/man \
		--disable-static

	make
	make DESTDIR=$PKG install
	install -m 0755 -D $SRC/conntrackd.rc $PKG/etc/rc.d/conntrackd
	install -m 0644 -D doc/stats/conntrackd.conf $PKG/etc/conntrackd/conntrackd.conf
}


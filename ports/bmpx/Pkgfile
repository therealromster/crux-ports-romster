# Description: Next generation rewrite of BMP.
# URL: http://beep-media-player.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Matt Housh, jaeger at morpheus dot net
# Depends on: boost cairomm gamin gst-plugins-bad gst-plugins-good gst-plugins-ugly hal libglade libglademm libmikmod libgsf librsvg libsexymm libsoup sqlite3 taglib xine-lib startup-notification

# Recommended:	
# Optional:	dbus, gconf, startup-notification, smlib
# Category:	audio, player

name=bmpx
version=0.40.0
release=1
source=(http://files.beep-media-player.org/releases/${version::4}/bmpx-$version.tar.bz2)

build() {
	cd bmpx-$version
	GST_PLUGINS_BASE_LIBS=/usr/lib
	
	./configure \
		--prefix=/usr \
		--libexecdir=/usr/lib \
		--mandir=/usr/man \
		--disable-dependency-tracking \
		--disable-nls \
		--enable-python \
		--enable-mp4v2 \
		--enable-modplug
		
	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/share/locale
	rm $PKG/usr/bin/bmp2
		
	ln -sf /usr/bin/beep-media-player-2 $PKG/usr/bin/bmp2
}


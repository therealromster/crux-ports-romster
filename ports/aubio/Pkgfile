# Description: A tool for extracting annotations from audio signals.
# URL: http://aubio.org/
# Maintainer: Danny Rawlins, crux at romster dot me
# Packager: Danny Rawlins, crux at romster dot me
# Depends on: ffmpeg fftw jack numpy

name=aubio
version=0.4.1
release=1
source=(http://aubio.org/pub/$name-$version.tar.bz2)

build() {
	cd $name-$version

	/usr/bin/python waf configure --prefix=/usr --enable-fftw3f
	/usr/bin/python waf build $MAKEFLAGS
	/usr/bin/python waf --destdir="$PKG" install

	# python interface
	cd python
	/usr/bin/python setup.py build
	/usr/bin/python setup.py install --root="$PKG" --optimize=1
}

# $Id: 
# Description: SMTP filter that allows you to check for viruses using the ClamAV anti-virus.
# URL: http://memberwebs.com/nielsen/software/clamsmtp/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Danny Rawlins, <romster@shortcircuit.net.au>
# Depends on: clamav

name=clamsmtp
version=1.9
release=1
source=(http://memberwebs.com/nielsen/software/clamsmtp/clamsmtp-$version.tar.gz \
	clamsmtpd.rc)

build () {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--mandir=/usr/man

	make
	make DESTDIR=$PKG install

	install -D -m 0755 $SRC/clamsmtpd.rc $PKG/etc/rc.d/clamsmptd
	install -D -m 0644 doc/clamsmtpd.conf $PKG/etc/clamsmtpd.conf

	sed -i -e 's/#User: clamav/User: clamav/' $PKG/etc/clamsmtpd.conf
}


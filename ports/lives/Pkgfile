# Description: LiVES is a Free, Open Source video editor and a VJ tool.
# URL: http://lives.sourceforge.net/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Danny Rawlins, romster at shortcircuit dot net dot au
# Depends on: win32-essential-codecs mplayer imagemagick gtk libjpeg sox
# Optional:	xmms dvdrtools python libsdl mencoder libmjpeg-tools libtheora jack-audio-connection-kit

name=lives
version=0.9.8.7
release=1
source=(http://www.xs4all.nl/%7Esalsaman/lives/current/LiVES-$version.tar.bz2)

build() {
	cd lives-$version

	./configure \
		--prefix=/usr \
		--disable-nls

	make
	make DESTDIR=$PKG install
	rm -r $PKG/usr/bin/lives
	ln -sf /usr/bin/lives-exe $PKG/usr/bin/lives
}


# Description: LiVES is a Free, Open Source video editor and a VJ tool.
# URL: http://lives.sourceforge.net/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Danny Rawlins, romster at shortcircuit dot net dot au
# Depends on: mplayer imagemagick gtk libjpeg sox
# Optional: win32-essential-codecs xmms dvdrtools python libsdl mencoder libmjpeg-tools libtheora jack-audio-connection-kit

name=lives
version=0.9.8.12
release=1
source=(http://www.xs4all.nl/%7Esalsaman/lives/current/LiVES-$version.tar.bz2 \
	lives-$version-install.patch)

build() {
	cd lives-$version

	patch -p 1 -i ../lives-$version-install.patch
	export CFLAGS="`echo $CFLAGS | sed -e 's/-O[s0-3]/-O3/'`"
	rm po/*.po

	sed -i \
		-e 's/USE_NLS=yes/USE_NLS=no/' \
		-e 's/ENABLE_NLS 1/ENABLE_NLS 0/' configure

	./configure \
		--prefix=/usr \
		--disable-nls

	make
	make DESTDIR=$PKG install
	rm -r $PKG/usr/share/doc
}


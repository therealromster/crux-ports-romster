# Description: LiVES is a Free, Open Source video editor and a VJ tool.
# URL: http://lives.sourceforge.net/
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Danny Rawlins, romster at shortcircuit dot net dot au
# Depends on: gtk imagemagick jack libavc1394 liboil libtheora libvisual libvorbis mjpegtools mplayer python sox

name=lives
version=1.3.12
release=1
source=(http://www.xs4all.nl/%7Esalsaman/lives/current/LiVES-$version.tar.bz2)

build() {
	cd $name-$version

	# brakes libweed
	CFLAGS="${CFLAGS//-flto/}" LDFLAGS="${LDFLAGS//-flto/}"

	./configure \
		--prefix=/usr \
		--disable-nls

	make
	make DESTDIR=$PKG install

	rm $PKG/usr/bin/lives
	ln -sf /usr/bin/lives-exe $PKG/usr/bin/lives

	rm -r $PKG/usr/share/doc
}

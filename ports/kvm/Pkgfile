# Description: Kernel-based Virtual Machine for Linux.
# URL: http://kvm.qumranet.com/kvmwiki
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Mikhail Kolesnik, mike at openbunker dot org
# Depends on: libsdl

name=kvm
version=70
release=1
up2date="lynx -dump http://sourceforge.net/project/showfiles.php?group_id=180599 |grep tar.gz |sed -ne 's/.*-\(.*\)\.t.*/\1/' -e '1 p'"
source=(http://dl.sourceforge.net/sourceforge/$name/$name-$version.tar.gz
		$name-depmod.diff)

build() {
	cd $name-$version
	patch -p 1 -i $SRC/$name-depmod.diff
	./configure --prefix=/usr 
	make
	make DESTDIR=$PKG install
	ln -s /usr/bin/qemu-system-x86_64 $PKG/usr/bin/qemu
	rm $PKG/usr/include/linux/kvm.h
}


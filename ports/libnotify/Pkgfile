# Description: Notification library from the Galago project.
# URL: http://galago-project.org/
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Danny Rawlins, monster dot romster at gmail dot com
# Depends on: dbus-glib gcc gtk+ xorg-libxcomposite xorg-xcb-util

name=libnotify
version=0.4.5
release=1
source="http://galago-project.org/files/releases/source/$name/$name-$version.tar.gz"

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--disable-static

	make
	make DESTDIR=$PKG install
	rm -r $PKG/usr/share/gtk-doc
}

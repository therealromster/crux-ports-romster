# Description: MTPFS is a FUSE filesystem based on libmtp that allows a mtp device to be browsed as if it were a normal external harddisk.
# URL: http://www.adebenham.com/mtpfs/
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Danny Rawlins, monster dot romster at gmail dot com
# Depends on: fuse glib libid3tag libmad libmtp libusb

name=mtpfs
version=0.9
release=1
source=(http://www.adebenham.com/debian/${name}_$version.orig.tar.gz)

build() {
	cd $name-$version.orig

	./configure \
		--prefix=/usr \
		--disable-static \
		--disable-dependency-tracking \

	make
	make DESTDIR=$PKG install
}

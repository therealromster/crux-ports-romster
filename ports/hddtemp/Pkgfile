# Description: Reads your hdd temperature using S.M.A.R.T values.
# URL: http://www.guzu.net/linux/hddtemp.php
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: sten, nick dot steeves at shaw dot ca
# Depends on: gettext

name=hddtemp
version=0.3-beta15
release=1
source=(http://www.guzu.net/files/hddtemp-$version.tar.bz2 \
	http://www.guzu.net/linux/hddtemp.db)

build() {
	cd hddtemp-$version

	./configure \
		--prefix=/usr \
		--with-db-path=/usr/etc/hddtemp.db \
		--disable-nls

	make
	make DESTDIR=$PKG install
	install -m 0644 -D ../hddtemp.db $PKG/usr/etc/hddtemp.db
}


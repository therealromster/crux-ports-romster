# Description: A simple zoomable image viewer or browser (ZUI) that allows you to directly zoom into large collection of tens of thousand of images from tiny thumbnails down to the images original size.
# URL: http://code.google.com/p/galapix/
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Danny Rawlins, monster dot romster at gmail dot com
# Depends on: openal sqlite3 libjpeg libpng scons glew imagemagick boost librsvg libexif opengl
# Optional: librsvg ufraw krita xcftools

name=galapix
version=0.1.2
release=1
source=(http://$name.googlecode.com/files/$name-$version.tar.bz2)

build() {
	cd $name-$version
	scons \
		destdir=$PKG \
		install_prefix=/usr \
		build

	scons install
	install -D -m 0655 build/galapix.gtk $PKG/usr/bin/galapix

}

# Description: An API for commonly used low-level network functions.
# URL: http://www.packetfactory.net/libnet/
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Mattias Hedenskog, mattias at hedenskog dot se
# Depends on: 

name=libnet
version=1.1.2.1
release=1
source=(http://fresh.t-systems-sfr.com/unix/src/privat2/libnet-$version.tar.gz)

build() {
	cd libnet
	./configure --prefix=/usr
	make
	make DESTDIR=$PKG install
	install -D -m 0755 libnet-config $PKG/usr/bin/libnet-config
}


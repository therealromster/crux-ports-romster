# Description: A lightweight RDF library with special support for LADSPA plugins.
# URL: https://github.com/swh/LRDF
# Maintainer: Danny Rawlins, crux at romster dot me
# Packager: Diederick de Vries, diederick at diederickdevries dot net
# Depends on: raptor ladspa

name=liblrdf
version=0.5.0
release=1
source=(https://github.com/swh/LRDF/tarball/$version/$name-$version.tar.gz)

build() {
	cd *-LRDF-*
	aclocal
	automake --add-missing
	./configure --prefix=/usr
	make
	make DESTDIR=$PKG install
}

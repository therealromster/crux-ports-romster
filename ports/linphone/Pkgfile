# Description:	Linphone is an internet phone or Voice Over IP phone (VoIP).
# URL: http://www.linphone.org/index.php/eng
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Ivan Rodionov, jean at mosk ru
# Depends on: bcg729 belle-sip libexosip2 libglade libsoup

name=linphone
version=3.7.0
release=1
source=(http://ftp.twaren.net/Unix/NonGNU//linphone/3.7.x/sources/$name-$version.tar.gz)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--libexecdir=/usr/lib/$name \
		--mandir=/usr/man \
		--disable-dependency-tracking \
		--disable-static \
		--disable-nls \
		--disable-manual \
		--enable-ipv6 \
		--enable-video \
		--enable-alsa \
		--disable-strict \
		--enable-external-mediastreamer \
		--enable-external-ortp \
		--enable-zrtp

	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/man/cs
}

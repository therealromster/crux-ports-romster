# Description: Java 2 Runtime Kit 32bit.
# URL:         http://www.oracle.com/technetwork/java/index.html
# Maintainer:  Danny Rawlins, monster dot romster at gmail dot com
# Packager:    Simone Rota, sip at crux dot nu
# Depends on: xorg-libxtst-32 xorg-libxi-32

##export MOZ_PLUGIN_PATH="/usr/lib/mozilla/plugins"  "${pkgdir}/etc/profile.d/"
 

name=jre-32
version=1.7.0_25
release=1
source=(file:///jre-7u25-linux-i586.tar.gz)

build() {
	cd jre$version

	install -d $PKG/usr/{bin,man,lib32/jre$version}
	mv bin lib plugin $PKG/usr/lib32/jre$version
	ln -s jre$version $PKG/usr/lib32/java
}

# Description: Intermediate Queueing Device, Iptables filter.
# URL: http://www.linuximq.net/
# Maintainer: Danny Rawlins, monster dot romster at gmail dot com
# Packager: Danny Rawlins, monster dot romster at gmail dot com
# Depends on: iproute2

name=iptables-imq
version=1.4.1-rc3
release=1
source=(ftp://ftp.netfilter.org/pub/iptables/iptables-$version.tar.bz2
	iptables-$version-imq.patch)
#	http://www.linuximq.net/patchs/iptables-1.4.0-imq.diff)

build() {
	cd iptables-$version
	patch -p 1 -i $SRC/iptables-$version-imq.patch
	chmod u+x extensions/.IMQ-test*
	
	./configure \
		--prefix=/usr \
		--libexecdir=/usr/lib \
		--mandir=/usr/man \
		--disable-dependency-tracking \
		--with-kernel="/usr/src/linux-`uname -r`" \
		--enable-devel \
		--enable-libipq

	make
	make DESTDIR=$PKG install
	chmod a-x $PKG/usr/lib/xtables/*.so
}


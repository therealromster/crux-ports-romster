# Description: Lightweight panel/taskbar w/launchers
# URL: https://gitlab.com/o9000/tint2
# Maintainer: Danny Rawlins, crux at romster dot me
# Depends on: cmake gtk imlib2 startup-notification xorg-libxdamage

name=tint2
version=20140504
release=1
source=(http://crux.ster.zone/downloads/$name/$name-$version-r652.tar.xz)

build() {
	cd $name

	install -d build
	cd build

	cmake \
		-DCMAKE_INSTALL_PREFIX=/usr \
		-DINSTALL_MANDIR=man ..

	make DESTDIR=$PKG install
	
	mv $PKG/usr/share/man $PKG/usr/man
}	

# Description: vim-tools including xxd.
# URL: http://www.vim.org/
# Maintainer: Danny Rawlins, crux at romster dot me
# Packager: CRUX System Team, core-ports at crux dot nu
# Depends on: vim

name=vim-tools
version=7.4.205
release=1
source=(http://crux.nu/files/distfiles/vim-$version.tar.xz)

build () {
    cd vim-$version/src

    ./configure --prefix=/usr \
                --mandir=/usr/man \
                --with-vim-name=vim \
                --without-x \
                --disable-gui \
                --enable-multibyte \
                --disable-gpm \
                --disable-nls

    make VIMRTDIR=
    make VIMRTDIR= DESTDIR=$PKG installtools
}

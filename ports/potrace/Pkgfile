# Description: A utility for tracing a bitmap, which means, transforming a bitmap into a smooth, scalable image.
# URL: http://potrace.sourceforge.net/
# Maintainer: Danny Rawlins, crux at romster dot me
# Packager: Simone Rota, sip at varlock dot com
# Depends on:

name=potrace
version=1.10
release=1
source="http://downloads.sourceforge.net/project/$name/$name/$version/$name-$version.tar.gz"

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--mandir=/usr/man

	make
	make DESTDIR=$PKG install
	rm -r $PKG/usr/share/doc
	rmdir $PKG/usr/share $PKG/usr/include $PKG/usr/lib
}

# Description: Multimedia utilities for the KDE integrated Xorg desktop.
# URL: http://www.kde.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: sten, nick dot steeves at shaw dot ca
# Depends on: kdebase, flac, taglib, libtunepimp, cdrtools, xine-lib, libsdl, k3b

name=kdemultimedia
version=3.5.8
release=1
source=(http://download.kde.org/stable/$version/src/kdemultimedia-$version.tar.bz2)

build() {
	cd kdemultimedia-$version

	./configure	\
		--prefix=/usr \
		--with-alsa \
		--disable-debug \
		--disable-dependency-tracking \
		--enable-final

	make
	make DESTDIR=$PKG install
	find $PKG/usr/share/apps -name 'README*' -delete
}


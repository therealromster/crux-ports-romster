# Description: An all-platform Edonkey client.
# URL: http://www.amule.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Daniel Walpole, daniel at walpole dot id dot au
# Depends on: wxgtk

name=amule
version=2.1.3
release=1
source=(http://download.berlios.de/amule/aMule-$version.tar.bz2 \
	amule-$version-wx.patch)

build() {
	cd aMule-$version

	export \
		CFLAGS="$CFLAGS -fno-strict-aliasing" \
		CXXFLAGS="$CXXFLAGS -fno-strict-aliasing"

	patch -p1 -i $SRC/amule-$version-wx.patch

	./configure \
		--prefix=/usr \
		--mandir=/usr/man \
		--disable-nls \
		--disable-debug \
		--enable-optimize \
		--enable-alc \
		--enable-alcc

	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/{share/doc,man/??}
}

# Description: WINE - Implementation of Windows DLL's and core.
# URL: http://www.winehq.com/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Daniel Walpole, daniel at walpole dot id dot au
# Depends on: fontforge
# Optional: openssl zlib alsa-lib cyrus-sasl lcms libxml2 libxslt openldap nvidia mesa3d xorg-libice xorg-libsm xorg-libx11 xorg-libxau xorg-libxcb xorg-libxdmcp xorg-libxext xorg-libxxf86vm xorg-font-msttcorefonts

name=wine
version=0.9.57
release=4
source=(http://ibiblio.org/pub/linux/system/emulators/wine/wine-$version.tar.bz2 \
	compile-info.patch)

build() {
	cd wine-$version
	patch -p 1 -i ../compile-info.patch

	export \
		CFLAGS="-O3 $(echo $CFLAGS |sed -e 's/-O[s0-3] //')" \
		CXXFLAGS="-O3 $(echo $CXXFLAGS |sed -e 's/-O[s0-3] //')"

	./configure \
		--prefix=/usr \
		--mandir=$PKG/usr/man \
		--disable-nls \
		--disable-oss

	make depend
	make
	make prefix=$PKG/usr install

	rm -r $PKG/usr/man/??.UTF-8
}


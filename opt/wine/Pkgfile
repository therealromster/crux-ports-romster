# Description: WINE - Implementation of Windows DLL's and core.
# URL: http://www.winehq.com/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Daniel Walpole, daniel at walpole dot id dot au
# Depends on: fontconfig hal-info lcms mesa3d xorg-libxcomposite xorg-libxcursor xorg-libxi xorg-libxrandr xorg-libxinerama

# Optional: xorg-font-msttcorefonts openldap cups sane libgphoto2

# pre-install xdelta ######################################
EXT='.tar.bz2'
OLD_VERSION=1.1.2-1
diff=(http://scguy318.freeshell.org/romster/linux
	http://www.dcaf-security.org/distfiles)
###########################################################

name=wine
version=1.1.3
release=1
url="http://ibiblio.org/pub/linux/system/emulators/$name"
up2date="lynx -dump '$url/?C=M;O=A' |grep '.tar.bz2' |sed -ne 's/.*$name-\(.*\)\.t.*/\1/' -e '$ p'"
#source=(http://scguy318.freeshell.org/romster/linux/wine-$version.tar.bz2)
source=($url/$name-$version.tar.bz2)

build() {
	cd $name-$version

	./configure \
		--verbose \
		--prefix=/usr \
		--mandir=$PKG/usr/man \
		--disable-nls \
		--disable-oss

	make depend
	make
	make prefix=$PKG/usr install
	rm -r $PKG/usr/man/??.UTF-8
}


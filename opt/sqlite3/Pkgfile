# Description: SQL database engine.
# URL: http://www.sqlite.org/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Jürgen Daubert, juergen dot daubert at t-online dot de
# Depends on: lzma ncurses readline

name=sqlite3
version=3.5.7
release=1

#source=(http://romster.shortcircuit.net.au/crux/source/sqlite3/sqlite-$version.tar.lzma)
source=(http://www.sqlite.org/sqlite-$version.tar.gz)

build() {
	cd sqlite-$version

	./configure \
		--prefix=/usr \
		--enable-threadsafe \
		--disable-tcl \
		--disable-static

	make
	make DESTDIR=$PKG install

	install -d $PKG/usr/man/man1
	install -m 0644 sqlite3.1 $PKG/usr/man/man1/
}


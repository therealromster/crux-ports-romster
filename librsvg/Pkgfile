# $Id:
# Description: An SVG library based on libart.
# URL: http://librsvg.sourceforge.net/
# Maintainer: Danny Rawlins, romster at shortcircuit dot net dot au
# Packager: Matt Housh, jaeger at morpheus dot net
# Depends on: gdk-pixbuf gtk libsvg

# Recommended:	
# Optional:	firefox, gnome-vfs, libgsf, libcroco
# Category:	library

name=librsvg
version=2.16.1
release=1
source=(http://ftp.gnome.org/pub/GNOME/sources/librsvg/${version%.*}/librsvg-$version.tar.bz2)

build() {
	cd librsvg-$version

	./configure \
		--prefix=/usr \
		--enable-gtk-theme \
		--enable-pixbuf-loader \
		--disable-gtk-doc \
		--disable-gnome-vfs \
		--with-svgz \
		--with-croco \
		--with-cairo

	make
	make DESTDIR=$PKG install
	rm -r $PKG/usr/{lib/mozilla,share/gtk-doc}
}


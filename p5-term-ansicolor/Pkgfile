# $Id:
# Description:	ANSI text atribute control module.
# URL:			http://search.cpan.org/author/RRA/
# Maintainer:	Danny Rawlins, <romster at shortcircuit dot net dot au>
# Packager:		Stoyan Zhekov <zhware@hotpop.com>
# Depends on:	perl

name=p5-term-ansicolor
version=1.11
release=1
source=(http://www.cpan.org/modules/by-module/Term/ANSIColor-$version.tar.gz)

build() {
	cd ANSIColor-$version
	perl Makefile.PL
	make OPTIMIZE="$CFLAGS"
	make DESTDIR=$PKG install
	
	# Remove perlcrap
	find $PKG \
		-name '.packlist' -or \
		-name '*.bs' -or \
		-name 'autosplit.ix' -or \
		-name 'perllocal.pod' | xargs rm

	# Remove empty directories
	find $PKG -depth -empty -exec rm -rf {} \;

	chown -R root:root $PKG
}

